# Foundry configuration — OP Sepolia track
# Reference: https://book.getfoundry.sh/reference/config/

[profile.default]
src             = "src"
out             = "out"
libs            = ["lib"]
test            = "test"
script          = "script"
cache_path      = "cache"
solc_version    = "0.8.24"
optimizer       = true
optimizer_runs  = 200
via_ir          = false
evm_version     = "paris"
auto_detect_solc = false
remappings = [
    "@openzeppelin/=lib/openzeppelin-contracts/",
    "forge-std/=lib/forge-std/src/",
]
fs_permissions = [
    { access = "read", path = "./" },
    { access = "read-write", path = "./broadcast" }
]
verbosity = 2

[profile.ci]
verbosity      = 3
fuzz.runs      = 1024
invariant.runs = 256

[rpc_endpoints]
op_sepolia = "${OP_SEPOLIA_RPC_URL}"
anvil      = "http://127.0.0.1:8545"

[etherscan]
op_sepolia = { key = "${OP_ETHERSCAN_API_KEY}", chain = 11155420, url = "https://api-sepolia-optimism.etherscan.io/api" }

[fmt]
line_length         = 120
tab_width           = 4
bracket_spacing     = true
int_types           = "long"
quote_style         = "double"
number_underscore   = "thousands"
multiline_func_header = "all"
